# Word-Cloud
#add words to stoplist if you want to remove words from wordcloud (like Bloomberg etc)
#you only need the .py and stoplist.txt in the folder with your pdfs.
#packages required in the .py
